module.exports = {
  // A list of paths to directories that Jest should use to search for files in
  roots: [
    __dirname + "/tests/bundle"
  ],

  // The test environment that will be used for testing
  testEnvironment: "node",
};
